apiVersion: v1
kind: Service
metadata:
    name: {{.Release.Name}}
    namespace: {{ .Values.commmon.namespace | default .Release.Namespace }}
spec:
    type: ExternalName
    externalName: httpbin.org